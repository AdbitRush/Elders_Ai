# 🚀 UPGRADES — Elders_Ai (games PWA)

**What this is:** a running inbox of upgrades / changes you (the owner) want made to
**Elders_Ai (games PWA)**. Drop requests here anytime — the next AI coding session reads this file
right after `HANDOFF.md` and implements the top unchecked items.

## For the owner (Or)
- Add anything you want changed under **📥 Requested** below — one line each, plain language.
- Rough is fine (e.g. "make the gallery load faster", "add an X button", "fix Y"). No need to be technical.
- Priority: put the most important at the top, or tag it `[P0]` / `[P1]`.

## For the next AI session — PROTOCOL (do this)
1. Read `HANDOFF.md` (current state) **then this file**.
2. Work the top unchecked `- [ ]` items in **📥 Requested** (respect `[P0]` > `[P1]` > rest).
3. For each finished item: change it to `- [x]`, move it into **✅ Done** with the date + a
   one-line note (commit hash if useful). Verify it per this repo's own rules before claiming done.
4. If a request is unclear or risky, leave it unchecked and add a `> Q:` line asking the owner.
5. Commit + push. Keep this file the single source of truth for "what to build next here."

---

## 📥 Requested  (newest / highest-priority at top)

From **Golden_Games_Implementation_Brief.pdf** (26 Aug 2026). Phase 1 is DONE —
see ✅ below. These are the remaining phases, in the brief's own priority order.
Do one phase per commit; a broken deploy on a static host means the site is down.

**Phase 2 — architecture**
- [x] Done 2026-08-26 (abb5d51) — see ✅ Phase 2 below.
- [x] Done 2026-08-26 (abb5d51) — see ✅ Phase 2 below.
- [ ] [P2] Verify the PWA is genuinely installable (manifest + icons + offline).
      Partly present already — audit before rebuilding.

**Artwork**
- [ ] [P1] Regenerate all 27 game card images with a better model. The brief —
      every file, what it should show, and why the current set reads as a mess —
      is in **`IMAGES.md`**. Read that before generating anything; the old
      prompts are what produced the pictures being replaced.

**Phase 3 — retention and reach**
- [ ] [P1] Printable puzzles. The print stylesheet already exists in
      `css/a11y.css`; what is missing is a "Print this puzzle" button on word
      search, sudoku and the word games, with the answer key on page 2. The brief
      rates this the most under-served feature in the niche — printables get
      photocopied and passed around care homes.
- [ ] [P2] Deterministic daily puzzle via mulberry32 seeded with YYYYMMDD so every
      player worldwide gets the same puzzle. A Daily Challenge banner already
      exists — check whether it is already date-seeded before building this.
- [ ] [P2] Move "no ads, no tracking" from the footer to a badge near the top.
      It is the single biggest differentiator against the ad-heavy incumbents and
      visitors currently never see it before they bounce.

**Deliberately NOT doing, with reasons** (do not re-add without re-reading these)
- The brief's 60×60px floor on *every* control breaks the 9×9 sudoku board and its
  ten-key pad on a phone. Standalone buttons are 48px; grid cells stay smaller.
- The brief's colour tokens (#fffdf7 bg, #1a1a1a ink, gold measured on white) are
  computed for a LIGHT theme. This build is dark navy — applying them would invert
  the design, not fix contrast. The real measured failures were fixed instead.
- Its §3.3 "rotate the admin token, PHP is being served as plain text" does not
  apply: **zero** PHP files have ever been committed to this repo
  (`git log --all --diff-filter=A` confirms). That warning is about the separate
  fashionhotspot project on a different host.

## ✅ Done — Phase 2 (2026-08-26, commit abb5d51)

Landed once as 46da782, reverted the same evening because the index.html it was
built on still carried unresolved conflict markers and an unclosed `<style>`
from an earlier merge — the site was already broken when it landed. Those were
fixed (49da64a, 7744fc6) and the work re-applied on the repaired file.

- [x] Lazy-load game code. `loadGame()` is async and appends one game's script
      on first open; the eager 27-tag block is gone. ~126 KB no longer downloads
      before the menu can draw. Verified: 0 game scripts on first paint.
- [x] 162 indexable pages at `/<lang>/<game>/` — name, what it trains, skills,
      how to play, button through to the game. Reciprocal hreflang across six
      languages + x-default, canonical, Open Graph, Game JSON-LD. Generated by
      `tools/build_pages.py` from index.html/lang-content.js/categories.js, so
      rerun it after changing any game text.
- [x] sitemap.xml (163 urls) + robots.txt.
- [x] Bug found on the way: `#klondike` was a dead deep link — `GAME_IDS` had 27
      entries, the hash router's `_VALID_GAMES` had 26. One list now.
- [x] Bug found on the way: sudoku was advertised as a "4×4 puzzle" in all six
      languages. It is a real 9×9.

Verified: 162/162 game × language renders on a local server with zero JS errors;
`#klondike` opens Klondike; every generated link resolves 200 on the deployed
`/Elders_Ai/` subpath. sw.js → v32.

## ✅ Done — Phase 1 (2026-08-26)

- [x] Trademark renames: tetris→blocks ("Falling Blocks"), simon→sequence, incl.
      assets, ids, piece colours off the classic trade dress, and the "TETRIS!"
      shout. Checked against nine spellings incl. Hebrew and Greek
      transliterations — Greek was still showing "Τέτρις".
- [x] One-time localStorage migration so existing high scores survive the rename.
- [x] `game_blocks_title` written in all six languages (it existed in two).
- [x] Scoreboard pill said "played/18" — now the real count.
- [x] Removed the GitHub NOTES.md edit link from the footer.
- [x] `css/a11y.css`: tap targets, a non-gold focus ring, measured contrast fixes,
      prefers-reduced-motion, large-print @media print.

---

## ✅ Done

- 2026-08-26 — Owner session (Telegram) round 2: share/invite upgraded — new domain (games.178-105-148-72.sslip.io), new brand (BrainPlay/משחקי מוח) in share texts, WhatsApp + Email + SMS + copy-link chooser via new navbar "Invite friends" button (all 6 languages); score share updated. SW cache v28→v29.
